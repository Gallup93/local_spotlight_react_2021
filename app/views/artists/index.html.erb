<p id="notice"><%= notice %></p>

<div class="location-form-container">
  <%= form_with url: "/locations", method: :post, remote: true do |form| %>
  <%= form.label :city, "DA CITY:" %>
  <%= form.text_field :city %>
  <%= form.label :city, "DA STATE:" %>
  <%= form.text_field :state %>
  <%= form.submit "Search" %>
  <% end %>
</div>


<div>
  <%= react_component 'ArtistsIndexWrap', artists: @artists, selected_spot_id: @selected_spot_id %>
</div>
